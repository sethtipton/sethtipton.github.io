{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci && PLAYWRIGHT_BROWSERS_PATH=0 npx playwright install --force chromium --no-shell",
  "buildCommand": "PLAYWRIGHT_BROWSERS_PATH=0 npm run build"
}
